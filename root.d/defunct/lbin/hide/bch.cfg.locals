#!/usr/bin/env bash

all () { ls ${BCH_BASE}; }

cfg () { all  | grep ^bch\.config\. ;  }
nnn () { all  | grep ^bch\.[0-9][0-9][0-9]\. ;  }


case $1. in
    all.|cfg.|nnn.)
        $*
        exit $?
        ;;
esac

cat >&2 << EOF

usage:
    bch.cfg.locals [all|cfg|nnn]
EOF
