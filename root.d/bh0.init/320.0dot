#!/usr/bin/env bash

0dot () {
    local zero=$1
    local link=$2
    [   ${zero}. == ${link}. ] && export dot=
    [   ${zero}. == ${link}. ] && export fin=exit
    [ ! ${zero}. == ${link}. ] && dot=.
    [ ! ${zero}. == ${link}. ] && fin=return
}
