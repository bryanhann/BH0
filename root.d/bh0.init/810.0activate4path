#!/usr/bin/env bash

0activate () {
    local x
    local y
    for x in activate activate.sh .bch/activate .bch/activate.sh; do
        y=$1/$x
        0bold ".  $y"
        #[ -f $y ] && 0bold ".  $y"

        #[ -f $y ] && . $y
    done
}
